# Grails & ReactAdminOnRest Demo

This is a demo of the admin-on-rest library for React.js works together with Grails API Server backend.

## How to run

In the project directory, you can run:

```
$ ./gradlew bootRun --parallel
```
